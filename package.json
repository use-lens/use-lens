{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "examples:start": "lerna run start --scope --",
    "prebuild": "lerna version --no-private",
    "build": "lerna run --scope '@use-lens/*' build",
    "publish": "yarn build && lerna publish --no-private --include-merged-tags"
  },
  "devDependencies": {
    "@graphql-codegen/cli": "^2.12.0",
    "@graphql-codegen/typescript": "^2.7.3",
    "@graphql-codegen/typescript-operations": "^2.5.3",
    "@types/node": "^18.7.16",
    "graphql": "^16.6.0",
    "lerna": "^5.5.0",
    "nx": "^14.7.3",
    "typescript": "^4.8.2"
  }
}
