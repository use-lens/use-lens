schema: lens-api.schema.graphql
documents: lens-api.documents.graphql
generates:
  ./src/lens-api/index.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
