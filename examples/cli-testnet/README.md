# CLI with `@testnet` example

> With the local development the example is using Yarn-Lerna workspace, so actual version is `@latest`
> Purpose of example to show how the repo is using `@testnet` in **package.json**.
